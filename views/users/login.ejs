<!DOCTYPE html>
<html lang="en">
<%-include('../partials/head')%>
<body>
<%-include('../partials/header')%>
    <main>
        <% if (locals && locals.error) { %>
            <p><%= error.msg %> </p>
        <% } %>
        <div class="card-login">   
            <form action="" method="POST">  
                <div class="msg-login">
                    <h4>Bienvenido! Ingresá tu e-mail y contraseña de The Book Store</h4>
                </div>
                    <div class="container-login">
                        <label for="mail">E-mail</label>
                        <input type="email" name="email" placeholder="Ingrese su e-mail" required>

                        <label for="password">Contraseña</label>
                        <input type="password" name="password" placeholder="Ingrese contraseña" required>

                        <div class="container-button">
                            <button type="submit">Ingresar</button>
                            <label>
                                <input type="checkbox" name="remember"> Recordar usuario
                            </label>
                        </div>
                        <div class="container-links-psw">
                            <ul type="none">
                                <li><a href="#">Olvidé mi contraseña</a></li>
                                <li><a href="/users/register">Crear cuenta</a></li>
                            </ul>
                        </div>
                    </div>                
            </form>
        </div>

    </main>
<%-include('../partials/footer')%>
</body>
</html>