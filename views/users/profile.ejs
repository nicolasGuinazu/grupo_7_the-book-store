<!DOCTYPE html>
<html lang="en">
<%-include('../partials/head')%>
<body>
<%-include('../partials/header')%>
    <main>
        
        <div class="card-profile">   
            <% if(locals.logged) { %>    
                <h1>Hola <%= locals.loggedUser.first_name %>!</h1>
                <img class="profile-image" src="/images/users/<%= locals.loggedUser.avatar%>">
                <% } %>
        </div>

    </main>
<%-include('../partials/footer')%>
</body>
</html>